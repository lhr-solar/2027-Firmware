# Validate CAN DBC / ID matrix (duplicates, codegen drift)
name: test-can-matrix
on:
  push:
  pull_request:
jobs:
  can-matrix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "TODO: run can/codegen validators"